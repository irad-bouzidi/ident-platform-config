version: 1
consumer:
  id: test-id
  display_name: Test Company
  country_codes:
    - de
products: []
redirects:
  safe-redirect-hosts:
    - test.com
security:
  inbound:
    auth:
      consumer_resolution:
        source: jwt
        claim: test
      required_scopes:
        - test
  outbound:
    webhook:
      mtls_profile: test
